# The app has no reflection-based serialisation, so the defaults suffice.
# Keep line numbers in stack traces from release builds.
-keepattributes SourceFile,LineNumberTable
-renamesourcefileattribute SourceFile
